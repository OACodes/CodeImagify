import { useState } from "react";
import Options from "./Options";

const CodeDisplay = ({background}) => {

    
    return (
        <div className={'w-2xl h-100 bg-blue-500 m-10 mt-20'}>
            
            {/* Display the options here, in Options */}
        </div>
        
    );
}

export default CodeDisplay