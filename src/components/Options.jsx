const Options = () => {
    // Add on the component of each option
    // Utilize Parent-Child compoent relationship
    return(
        <div className={'w-90 h-120 bg-[#fcfcfd] mt-20 border-1 border-grey-500 rounded-sm'}>
            <h1 className={'text-base'}>BACKGROUND</h1>
        </div>
    );
}

export default Options